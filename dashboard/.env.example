# Dashboard — copy to dashboard/.env and fill in your Supabase project values

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# The Chrome Extension ID — found on chrome://extensions after loading the extension.
# Required for the "Sync session to extension" feature on the auth callback page.
# Leave blank to disable that feature.
VITE_EXTENSION_ID=
