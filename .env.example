# Extension — copy to .env and fill in your Supabase project values
# These are embedded at build time (Vite replaces import.meta.env.*)
# The anon key is safe to expose in the client — RLS enforces access control.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# URL of the web dashboard (used for Google OAuth redirect and "Open Dashboard" links)
# Dev:  http://localhost:5174
# Prod: https://your-dashboard-domain.com
VITE_DASHBOARD_URL=http://localhost:5174
